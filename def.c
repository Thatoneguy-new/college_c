#include <stdio.h>

    typedef struct point{
        int x;
        int y;
    } point;

    typeof int (*f ptr) (const point* , const point*);

    int main() {
        typeof int integers;
        typeof char chararray[100];
        ftr = functionpointer;
        point point = {10, 20};
        chararray name = "priyam";
        printf("%d %d\n", point.x, point.y);

        return 0;
    }